name: Feature

on:
  push:
    branches:
      - feature/**

jobs:
  build-and-deploy:
    name: build-and-deploy
    uses: ./.github/workflows/build-and-deploy.yaml
    with:
      environment_name: development
